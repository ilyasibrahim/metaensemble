[project]
name = "oss-fixture-legacy"
version = "1.0.0"
description = "Recfile parsing, validation, and rendering in one legacy module."
requires-python = ">=3.10"

[tool.pytest.ini_options]
addopts = "-q"
